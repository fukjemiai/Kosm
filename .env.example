# Kosm – root .env (shared)
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=kosm
DB_PASSWORD=kosm_dev
DB_NAME=kosm

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=kosm

# Mail (MailHog dev)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_FROM=noreply@kosm.local

# Guest token
GUEST_TOKEN_SECRET=dev-secret-change-in-production
